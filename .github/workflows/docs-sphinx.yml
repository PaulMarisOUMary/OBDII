name: Build Docs

on:
  push:
    paths:
      - "obdii/**"
      - "**/*.py"
      - "!tests/**/*.py"
      - "requirements.txt"
      - "pyproject.toml"
    branches: [ main ]
  pull_request:
    paths:
      - "obdii/**"
      - "**/*.py"
      - "!tests/**/*.py"
      - "requirements.txt"
      - "pyproject.toml"
    branches: [ main ]

  workflow_dispatch:

jobs:
  sphinx-docs:
    name: Build Documentation 📄
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: 3.x
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install -e .[docs]

      - name: Build documentation
        working-directory: docs
        run: |
          sphinx-build -b html -j auto -a -n -T -W --keep-going source build/html

      - name: Check broken links
        continue-on-error: true
        working-directory: docs
        run: sphinx-build -b linkcheck -j auto source build/linkcheck
